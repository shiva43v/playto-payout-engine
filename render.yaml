services:
  # Backend Web Service (Combined Web + Celery for Free Tier)
  - type: web
    name: playto-backend
    env: python
    plan: free
    buildCommand: python backend/build.sh.py
    startCommand: bash start.sh
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: playto-db
          property: connectionString
      - key: REDIS_URL
        fromService:
          type: redis
          name: playto-redis
          property: connectionString
      - key: SECRET_KEY
        generateValue: true
      - key: DEBUG
        value: false
      - key: SEED_DB
        value: true

  # Frontend Static Site
  - type: web
    name: playto-frontend
    runtime: static
    buildCommand: cd frontend && npm install && npm run build
    staticPublishPath: frontend/dist
    envVars:
      - key: VITE_API_URL
        value: https://playto-backend.onrender.com/api/v1

  # Redis Service
  - type: redis
    name: playto-redis
    plan: free
    ipAllowList: []

databases:
  - name: playto-db
    plan: free
